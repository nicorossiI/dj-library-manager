node_modules/
dist/
.env
*.log
.DS_Store
Thumbs.db

# Claude Code runtime state (locale, non condiviso)
.claude/

# Binari bundled (sostituire dopo clone)
assets/bin/fpcalc.exe
assets/bin/fpcalc-mac

# Chiavi API embedded — file admin-only con chiavi reali (NON committare!)
# Il template committed è src/constants/embedded-keys.js (vuoto).
# Crea `embedded-keys.local.js` con le tue chiavi per farle finire nel build.
src/constants/embedded-keys.local.js
